import { createStyles, createTheme } from "@mui/material";


const styles = (theme) => (
    createStyles({
        buttonStyle: {
            marginTop: "200px",
          }
    })
)
  

export default styles;